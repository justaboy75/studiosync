services:
  # Database: PostgreSQL
  db:
    image: postgres:16-alpine
    container_name: studiosync-db
    restart: always
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

  # Backend: PHP + Nginx (API Server)
  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile
    restart: always
    environment:
      DB_HOST: db
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASS: ${DB_PASS}
      DB_PORT: ${DB_PORT}
    container_name: studiosync-api
    ports:
      - "8080:80"
    volumes:
      - ./backend:/var/www/html
      - ./uploads:/var/www/html/uploads
    depends_on:
      - db

  # Frontend: Next.js / React
  frontend:
    build:
      context: ./frontend
      args:
        - NEXT_PUBLIC_API_URL=https://app.just-code.it/api
    restart: always
    container_name: studiosync-ui
    ports:
      - "3000:3000"
    #volumes:
    #  - ./frontend:/app
    #  - /app/node_modules
    environment:
      - NEXT_PUBLIC_API_URL=https://app.just-code.it/api
      - NODE_ENV=production
    command: npm run start -- -H 0.0.0.0
    depends_on:
      - backend
    stdin_open: true
    tty: true

volumes:
  db_data: